/**
 * ,--.,------. ,----.   ,------.,--.  ,--. 
 * |  ||  .---''  .-.|   |  .---'|  ,'.|  | 
 * |  ||  `--, |  | .---.|  `--, |  |' '  | 
 * |  ||  `---.'  '--'  ||  `---.|  | `   | 
 * `--'`------' `------' `------'`--'  `--' 
 * 
 * This file is generated by iegen on 05/05/2022-13:33.
 * Please do not change it manually.
 */

package com.examples.inheritance

import com.examples.iegen.alias.*
import com.examples.iegen.exceptionUtils.*
import com.examples.iegen.exception_helpers.*

/**
 * A base class for checking single branch non-polymorphic inheritance cases
 */
open class Doctor
internal constructor(obj: IEGenObject) : AutoCloseable {
    companion object {
        init {
            System.loadLibrary("wrapper_jni")
        }
        
        protected fun construct_helper(doctor_name: String): Long {
            val id = jConstructor(doctor_name)
            return id
        }

        @JvmStatic
        private external fun jConstructor(doctor_name: String): Long
        const val iegenCxxTypeName: String = "iegen::example::Doctor"
    }
    
    protected var iegObj = obj
    private var refs: MutableList<Any> = mutableListOf()

    fun keepIEGenReference(ref: Any) {
        refs.add(ref)
    }
    
    open val id: Long
        get() {
            if (iegObj.id == 0L) {
                throw RuntimeException("Object is not allocated")
            }
            return iegObj.id
        }
    
    constructor(doctor_name: String): this(IEGenObject(construct_helper(doctor_name), true)) {
    }

    override fun close() {
        if (iegObj.owner && iegObj.id != 0L) {
            jFinalize(iegObj.id)
            iegObj.id = 0L
        }
    }

    /**
     * Finalize and deletes the object
     */
    protected fun finalize() {
        close()
    }
    private external fun jFinalize(id: Long): Unit
}

open class Surgeon
internal constructor(obj: IEGenObject) : Doctor(obj) {
    companion object {
        
        protected fun construct_helper(surgeon_name: String): Long {
            val id = jConstructor(surgeon_name)
            return id
        }

        @JvmStatic
        private external fun jConstructor(surgeon_name: String): Long
        const val iegenCxxTypeName: String = "iegen::example::Surgeon"
    }
    
    
    constructor(surgeon_name: String): this(IEGenObject(construct_helper(surgeon_name), true)) {
    }
}


/**
 * A base class for checking multibranch mixed polymorphic inheritance cases
 */
interface IGoodVirtualDoctor : AutoCloseable {
    val id: Long
    fun keepIEGenReference(ref: Any)
    
    

    companion object {
        public fun iegenConstructObject(id: Long, owner: Boolean = false): IGoodVirtualDoctor {
            val idType = jGettypebyid(id)
            when (idType) {
                GoodYoungDoctor.iegenCxxTypeName -> return GoodYoungDoctor(IEGenObject(id, owner))
                else -> return GoodVirtualDoctorImpl(IEGenObject(id, owner))
            }
        }
    }
}



open class GoodVirtualDoctorImpl
internal constructor(obj : IEGenObject) : IGoodVirtualDoctor {
    companion object {
        init {
            System.loadLibrary("wrapper_jni")
        }
        
        const val iegenCxxTypeName: String = "iegen::example::GoodVirtualDoctor"
    }

    protected var iegObj = obj
    private var refs: MutableList<Any> = mutableListOf()

    override fun keepIEGenReference(ref: Any) {
        refs.add(ref)
    }

    override val id: Long
        get() {
            if (iegObj.id == 0L) {
                throw RuntimeException("Object is not allocated")
            }
            return iegObj.id
        }

    override fun close() {
        if (iegObj.owner && iegObj.id != 0L) {
            jFinalize(iegObj.id)
            iegObj.id = 0L
        }
    }

    /**
    * Finalize and deletes the object
    */
    protected fun finalize() {
        close()
    }
    
    ///// External wrapper functions ////////////
    private external fun jFinalize(id: Long): Unit
}

open class GoodDoctor
internal constructor(obj: IEGenObject) : AutoCloseable {
    companion object {
        init {
            System.loadLibrary("wrapper_jni")
        }
        
        protected fun construct_helper(doctor_name: String): Long {
            val id = jConstructor(doctor_name)
            return id
        }

        @JvmStatic
        private external fun jConstructor(doctor_name: String): Long
        const val iegenCxxTypeName: String = "iegen::example::GoodDoctor"
    }
    
    protected var iegObj = obj
    private var refs: MutableList<Any> = mutableListOf()

    fun keepIEGenReference(ref: Any) {
        refs.add(ref)
    }
    
    open val id: Long
        get() {
            if (iegObj.id == 0L) {
                throw RuntimeException("Object is not allocated")
            }
            return iegObj.id
        }
    
    constructor(doctor_name: String): this(IEGenObject(construct_helper(doctor_name), true)) {
    }

    override fun close() {
        if (iegObj.owner && iegObj.id != 0L) {
            jFinalize(iegObj.id)
            iegObj.id = 0L
        }
    }

    /**
     * Finalize and deletes the object
     */
    protected fun finalize() {
        close()
    }
    private external fun jFinalize(id: Long): Unit
}

/**
 * Polymorphic subclass
 */
open class GoodYoungDoctor
internal constructor(obj: IEGenObject) : IGoodVirtualDoctor, GoodDoctor(obj) {
    companion object {
        
        protected fun construct_helper(doctor_name: String): Long {
            val id = jConstructor(doctor_name)
            return id
        }

        @JvmStatic
        private external fun jConstructor(doctor_name: String): Long
        const val iegenCxxTypeName: String = "iegen::example::GoodYoungDoctor"
    }
    
    override val id: Long
        get() {
            if (iegObj.id == 0L) {
                throw RuntimeException("Object is not allocated")
            }
            return iegObj.id
        }
    
    constructor(doctor_name: String): this(IEGenObject(construct_helper(doctor_name), true)) {
    }
}

/**
 * Non-polymorphic subclass
 */
open class GoodOldDoctor
internal constructor(obj: IEGenObject) : GoodDoctor(obj) {
    companion object {
        
        protected fun construct_helper(doctor_name: String): Long {
            val id = jConstructor(doctor_name)
            return id
        }

        @JvmStatic
        private external fun jConstructor(doctor_name: String): Long
        const val iegenCxxTypeName: String = "iegen::example::GoodOldDoctor"
    }
    
    
    constructor(doctor_name: String): this(IEGenObject(construct_helper(doctor_name), true)) {
    }
}

open class DoctorInfo
internal constructor(obj: IEGenObject) : AutoCloseable {
    companion object {
        init {
            System.loadLibrary("wrapper_jni")
        }
        
        protected fun construct_helper(s: Surgeon): Long {
            val kotlintojdks = s.id
            val id = jConstructor(kotlintojdks)
            return id
        }

        @JvmStatic
        private external fun jConstructor(s: Long): Long

        protected fun construct_helper(d: GoodYoungDoctor): Long {
            val kotlintojdkd = d.id
            val id = jConstructor_1(kotlintojdkd)
            return id
        }

        @JvmStatic
        private external fun jConstructor_1(d: Long): Long
        const val iegenCxxTypeName: String = "iegen::example::DoctorInfo"
    }
    
    protected var iegObj = obj
    private var refs: MutableList<Any> = mutableListOf()

    fun keepIEGenReference(ref: Any) {
        refs.add(ref)
    }
    
    open val id: Long
        get() {
            if (iegObj.id == 0L) {
                throw RuntimeException("Object is not allocated")
            }
            return iegObj.id
        }
    
    constructor(s: Surgeon): this(IEGenObject(construct_helper(s), true)) {
    }

    constructor(d: GoodYoungDoctor): this(IEGenObject(construct_helper(d), true)) {
    }
    
    fun getDoctorName(d: Doctor): String {
        val kotlintojdkd = d.id
        val result = jGetdoctorname(id, kotlintojdkd)
        
        return result
    }

    fun getSurgeonName(s: Surgeon): String {
        val kotlintojdks = s.id
        val result = jGetsurgeonname(id, kotlintojdks)
        
        return result
    }

    fun getDoctor(): Doctor {
        val result = jGetdoctor(id)
        val jdktokotlinresult = Doctor(IEGenObject(result))
        return jdktokotlinresult
    }

    fun getGoodDoctor(): GoodDoctor {
        val result = jGetgooddoctor(id)
        val jdktokotlinresult = GoodDoctor(IEGenObject(result))
        return jdktokotlinresult
    }

    fun getGoodVirtualDoctor(): IGoodVirtualDoctor {
        val result = jGetgoodvirtualdoctor(id)
        
        val jdktokotlinresult : IGoodVirtualDoctor
        jdktokotlinresult = IGoodVirtualDoctor.iegenConstructObject(result)
        return jdktokotlinresult
    }

    fun getGoodDoctorName(d: GoodDoctor): String {
        val kotlintojdkd = d.id
        val result = jGetgooddoctorname(id, kotlintojdkd)
        
        return result
    }

    override fun close() {
        if (iegObj.owner && iegObj.id != 0L) {
            jFinalize(iegObj.id)
            iegObj.id = 0L
        }
    }

    /**
     * Finalize and deletes the object
     */
    protected fun finalize() {
        close()
    }

    ///// External wrapper functions ////////////
    private external fun jGetdoctorname(id: Long, d: Long): String
    private external fun jGetsurgeonname(id: Long, s: Long): String
    private external fun jGetdoctor(id: Long): Long
    private external fun jGetgooddoctor(id: Long): Long
    private external fun jGetgoodvirtualdoctor(id: Long): Long
    private external fun jGetgooddoctorname(id: Long, d: Long): String
    private external fun jFinalize(id: Long): Unit
}

private external fun jGettypebyid(id: Long): String
