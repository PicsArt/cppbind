/**
 * ,--.,------. ,----.   ,------.,--.  ,--. 
 * |  ||  .---''  .-.|   |  .---'|  ,'.|  | 
 * |  ||  `--, |  | .---.|  `--, |  |' '  | 
 * |  ||  `---.'  '--'  ||  `---.|  | `   | 
 * `--'`------' `------' `------'`--'  `--' 
 * 
 * This file is generated by iegen on 05/05/2022-13:20.
 * Please do not change it manually.
 */

import CWrapper
import Foundation

public class SystemError: StdException {
  /// internal main initializer
  internal required init(_ _cself: IEGenCObject, _ _owner: Bool = false) {
    super.init(_cself, _owner)
  }

  /// internal dummy initializer to prevent automatic initializer inheritance
  internal init(_cself: IEGenCObject, _self: SystemError) {
    fatalError("A dummy internal initializer should not be called.")
  }

  /**
   * comments
   * 
   */
  public convenience init(message: String) {
    let swifttoscmessage = strdup(message)!
    var iegenErr = IEGenCObject()
    self.init(create_IegenExample_SystemError(swifttoscmessage, &iegenErr), true)
    if iegenErr.type != nil {
      let errorType = String(cString: iegenErr.type!)
      switch errorType {
      case ("std::exception"):
        let excObj = StdException(iegenErr, true)
        ExceptionHandler.handleUncaughtException(excObj.what())
      default:
        iegenErr.type.deallocate()
        ExceptionHandler.handleUncaughtException("Uncaught Exception")
      }
    }
  }

  /**
   * comments
   * 
   */
  public override func what() -> String {

    var iegenErr = IEGenCObject()
    let result = _func_IegenExample_SystemError_what(cself, &iegenErr)
    if iegenErr.type != nil {
      let errorType = String(cString: iegenErr.type!)
      switch errorType {
      case ("std::exception"):
        let excObj = StdException(iegenErr, true)
        ExceptionHandler.handleUncaughtException(excObj.what())
      default:
        iegenErr.type.deallocate()
        ExceptionHandler.handleUncaughtException("Uncaught Exception")
      }
    }
    let sctoswiftresult = String(cString: result)
    return sctoswiftresult
  }

  override class var iegenCxxTypeName : String { return "iegen::example::SystemError" }

  override class func iegenConstructObject(_ iegenObj: IEGenCObject, _ owner: Bool = false) -> SystemError {
    let typeName = String(cString: iegenObj.type)
    switch(typeName) {
    case(FileError.iegenCxxTypeName):
      return FileError(iegenObj, owner)
    default:
      return SystemError(iegenObj, owner)
    }
  }
}

public class FileError: SystemError {
  /// internal main initializer
  internal required init(_ _cself: IEGenCObject, _ _owner: Bool = false) {
    super.init(_cself, _owner)
  }

  public convenience init(message: String) {
    let swifttoscmessage = strdup(message)!
    var iegenErr = IEGenCObject()
    self.init(create_IegenExample_FileError(swifttoscmessage, &iegenErr), true)
    if iegenErr.type != nil {
      let errorType = String(cString: iegenErr.type!)
      switch errorType {
      case ("std::exception"):
        let excObj = StdException(iegenErr, true)
        ExceptionHandler.handleUncaughtException(excObj.what())
      default:
        iegenErr.type.deallocate()
        ExceptionHandler.handleUncaughtException("Uncaught Exception")
      }
    }
  }

  /**
   * comments
   * 
   */
  public override func what() -> String {

    var iegenErr = IEGenCObject()
    let result = _func_IegenExample_FileError_what(cself, &iegenErr)
    if iegenErr.type != nil {
      let errorType = String(cString: iegenErr.type!)
      switch errorType {
      case ("std::exception"):
        let excObj = StdException(iegenErr, true)
        ExceptionHandler.handleUncaughtException(excObj.what())
      default:
        iegenErr.type.deallocate()
        ExceptionHandler.handleUncaughtException("Uncaught Exception")
      }
    }
    let sctoswiftresult = String(cString: result)
    return sctoswiftresult
  }

  override class var iegenCxxTypeName : String { return "iegen::example::FileError" }
}
