/**
 * ,--.,------. ,----.   ,------.,--.  ,--. 
 * |  ||  .---''  .-.|   |  .---'|  ,'.|  | 
 * |  ||  `--, |  | .---.|  `--, |  |' '  | 
 * |  ||  `---.'  '--'  ||  `---.|  | `   | 
 * `--'`------' `------' `------'`--'  `--' 
 * 
 * This file is generated by iegen on 03/30/2022-09:24.
 * Please do not change it manually.
 */

import CWrapper
import Foundation

/**
 * A test example for return value policies. This is not used in documentation.
 */
public class SwValue {

  public let cself: IEGenCObject
  public let owner: Bool
  private var refs: [Any]

  // internal main initializer
  internal required init(_ _cself: IEGenCObject, _ _owner: Bool = false) {
    self.cself = _cself
    self.owner = _owner
    self.refs = []
  }

  deinit {
    release_IegenExample_SwValue(cself, owner)
  }

  public func keepIEGenReference(_ object: Any) {
    self.refs.append(object)
  }

  public convenience init(name: String) {
    let swifttoscname = strdup(name)!
    var iegenErr = IEGenCObject()
    self.init(create_IegenExample_SwValue(swifttoscname, &iegenErr), true)
    if iegenErr.type != nil {
      let errorType = String(cString: iegenErr.type!)
      switch errorType {
      case ("std::exception"):
        let excObj = StdException(iegenErr, true)
        ExceptionHandler.handleUncaughtException(excObj.what())
      default:
        iegenErr.type.deallocate()
        ExceptionHandler.handleUncaughtException("Uncaught Exception")
      }
    }
  }

  public var name: String {
    get {
      var iegenErr = IEGenCObject()
      let result = _prop_get_IegenExample_SwValue_name(cself, &iegenErr)
      if iegenErr.type != nil {
        let errorType = String(cString: iegenErr.type!)
        switch errorType {
        case ("std::exception"):
          let excObj = StdException(iegenErr, true)
          ExceptionHandler.handleUncaughtException(excObj.what())
        default:
          iegenErr.type.deallocate()
          ExceptionHandler.handleUncaughtException("Uncaught Exception")
        }
      }
      let sctoswiftresult = String(cString: result)
      defer {
        result.deallocate()
      }
      return sctoswiftresult
    }

    set(value) {
      let swifttoscvalue = strdup(value)!
      var iegenErr = IEGenCObject()
      _prop_set_IegenExample_SwValue_name(cself, swifttoscvalue, &iegenErr)
      if iegenErr.type != nil {
        let errorType = String(cString: iegenErr.type!)
        switch errorType {
        case ("std::exception"):
          let excObj = StdException(iegenErr, true)
          ExceptionHandler.handleUncaughtException(excObj.what())
        default:
          iegenErr.type.deallocate()
          ExceptionHandler.handleUncaughtException("Uncaught Exception")
        }
      }
    }
  }

}

public class ValueSharedRef {

  public let cself: IEGenCObject
  public let owner: Bool
  private var refs: [Any]

  // internal main initializer
  internal required init(_ _cself: IEGenCObject, _ _owner: Bool = false) {
    self.cself = _cself
    self.owner = _owner
    self.refs = []
  }

  deinit {
    release_IegenExample_ValueSharedRef(cself, owner)
  }

  public func keepIEGenReference(_ object: Any) {
    self.refs.append(object)
  }

  public convenience init(name: String) {
    let swifttoscname = strdup(name)!
    var iegenErr = IEGenCObject()
    self.init(create_IegenExample_ValueSharedRef(swifttoscname, &iegenErr), true)
    if iegenErr.type != nil {
      let errorType = String(cString: iegenErr.type!)
      switch errorType {
      case ("std::exception"):
        let excObj = StdException(iegenErr, true)
        ExceptionHandler.handleUncaughtException(excObj.what())
      default:
        iegenErr.type.deallocate()
        ExceptionHandler.handleUncaughtException("Uncaught Exception")
      }
    }
  }

  public var name: String {
    get {
      var iegenErr = IEGenCObject()
      let result = _prop_get_IegenExample_ValueSharedRef_name(cself, &iegenErr)
      if iegenErr.type != nil {
        let errorType = String(cString: iegenErr.type!)
        switch errorType {
        case ("std::exception"):
          let excObj = StdException(iegenErr, true)
          ExceptionHandler.handleUncaughtException(excObj.what())
        default:
          iegenErr.type.deallocate()
          ExceptionHandler.handleUncaughtException("Uncaught Exception")
        }
      }
      let sctoswiftresult = String(cString: result)
      defer {
        result.deallocate()
      }
      return sctoswiftresult
    }

    set(value) {
      let swifttoscvalue = strdup(value)!
      var iegenErr = IEGenCObject()
      _prop_set_IegenExample_ValueSharedRef_name(cself, swifttoscvalue, &iegenErr)
      if iegenErr.type != nil {
        let errorType = String(cString: iegenErr.type!)
        switch errorType {
        case ("std::exception"):
          let excObj = StdException(iegenErr, true)
          ExceptionHandler.handleUncaughtException(excObj.what())
        default:
          iegenErr.type.deallocate()
          ExceptionHandler.handleUncaughtException("Uncaught Exception")
        }
      }
    }
  }

}

public class ValuesHolder {

  public let cself: IEGenCObject
  public let owner: Bool
  private var refs: [Any]

  // internal main initializer
  internal required init(_ _cself: IEGenCObject, _ _owner: Bool = false) {
    self.cself = _cself
    self.owner = _owner
    self.refs = []
  }

  deinit {
    release_IegenExample_ValuesHolder(cself, owner)
  }

  public func keepIEGenReference(_ object: Any) {
    self.refs.append(object)
  }

  public convenience init() {
    var iegenErr = IEGenCObject()
    self.init(create_IegenExample_ValuesHolder(&iegenErr), true)
    if iegenErr.type != nil {
      let errorType = String(cString: iegenErr.type!)
      switch errorType {
      case ("std::exception"):
        let excObj = StdException(iegenErr, true)
        ExceptionHandler.handleUncaughtException(excObj.what())
      default:
        iegenErr.type.deallocate()
        ExceptionHandler.handleUncaughtException("Uncaught Exception")
      }
    }
  }

  public convenience init(valuePtr: SwValue, valueRef: ValueSharedRef) {
    let swifttoscvaluePtr = valuePtr.cself
    let swifttoscvalueRef = valueRef.cself
    var iegenErr = IEGenCObject()
    self.init(create_IegenExample_ValuesHolder_1(swifttoscvaluePtr, swifttoscvalueRef, &iegenErr), true)
    if iegenErr.type != nil {
      let errorType = String(cString: iegenErr.type!)
      switch errorType {
      case ("std::exception"):
        let excObj = StdException(iegenErr, true)
        ExceptionHandler.handleUncaughtException(excObj.what())
      default:
        iegenErr.type.deallocate()
        ExceptionHandler.handleUncaughtException("Uncaught Exception")
      }
    }
  }

  public func getPtrDefault() -> SwValue {

    var iegenErr = IEGenCObject()
    let result = _func_IegenExample_ValuesHolder_getPtrDefault(cself, &iegenErr)
    if iegenErr.type != nil {
      let errorType = String(cString: iegenErr.type!)
      switch errorType {
      case ("std::exception"):
        let excObj = StdException(iegenErr, true)
        ExceptionHandler.handleUncaughtException(excObj.what())
      default:
        iegenErr.type.deallocate()
        ExceptionHandler.handleUncaughtException("Uncaught Exception")
      }
    }
    var sctoswiftresult: SwValue
    sctoswiftresult = SwValue(result, true)
    return sctoswiftresult
  }

  public func getPtrCopy() -> SwValue {

    var iegenErr = IEGenCObject()
    let result = _func_IegenExample_ValuesHolder_getPtrCopy(cself, &iegenErr)
    if iegenErr.type != nil {
      let errorType = String(cString: iegenErr.type!)
      switch errorType {
      case ("std::exception"):
        let excObj = StdException(iegenErr, true)
        ExceptionHandler.handleUncaughtException(excObj.what())
      default:
        iegenErr.type.deallocate()
        ExceptionHandler.handleUncaughtException("Uncaught Exception")
      }
    }
    var sctoswiftresult: SwValue
    sctoswiftresult = SwValue(result, true)
    return sctoswiftresult
  }

  public func getPtrMove() -> SwValue {

    var iegenErr = IEGenCObject()
    let result = _func_IegenExample_ValuesHolder_getPtrMove(cself, &iegenErr)
    if iegenErr.type != nil {
      let errorType = String(cString: iegenErr.type!)
      switch errorType {
      case ("std::exception"):
        let excObj = StdException(iegenErr, true)
        ExceptionHandler.handleUncaughtException(excObj.what())
      default:
        iegenErr.type.deallocate()
        ExceptionHandler.handleUncaughtException("Uncaught Exception")
      }
    }
    var sctoswiftresult: SwValue
    sctoswiftresult = SwValue(result, true)
    return sctoswiftresult
  }

  public func getPtrReference() -> SwValue {

    var iegenErr = IEGenCObject()
    let result = _func_IegenExample_ValuesHolder_getPtrReference(cself, &iegenErr)
    if iegenErr.type != nil {
      let errorType = String(cString: iegenErr.type!)
      switch errorType {
      case ("std::exception"):
        let excObj = StdException(iegenErr, true)
        ExceptionHandler.handleUncaughtException(excObj.what())
      default:
        iegenErr.type.deallocate()
        ExceptionHandler.handleUncaughtException("Uncaught Exception")
      }
    }
    var sctoswiftresult: SwValue
    sctoswiftresult = SwValue(result)
    return sctoswiftresult
  }

  public func getPtrAutomatic() -> SwValue {

    var iegenErr = IEGenCObject()
    let result = _func_IegenExample_ValuesHolder_getPtrAutomatic(cself, &iegenErr)
    if iegenErr.type != nil {
      let errorType = String(cString: iegenErr.type!)
      switch errorType {
      case ("std::exception"):
        let excObj = StdException(iegenErr, true)
        ExceptionHandler.handleUncaughtException(excObj.what())
      default:
        iegenErr.type.deallocate()
        ExceptionHandler.handleUncaughtException("Uncaught Exception")
      }
    }
    var sctoswiftresult: SwValue
    sctoswiftresult = SwValue(result, true)
    return sctoswiftresult
  }

  public func getPtrAutomaticReference() -> SwValue {

    var iegenErr = IEGenCObject()
    let result = _func_IegenExample_ValuesHolder_getPtrAutomaticReference(cself, &iegenErr)
    if iegenErr.type != nil {
      let errorType = String(cString: iegenErr.type!)
      switch errorType {
      case ("std::exception"):
        let excObj = StdException(iegenErr, true)
        ExceptionHandler.handleUncaughtException(excObj.what())
      default:
        iegenErr.type.deallocate()
        ExceptionHandler.handleUncaughtException("Uncaught Exception")
      }
    }
    var sctoswiftresult: SwValue
    sctoswiftresult = SwValue(result)
    return sctoswiftresult
  }

  public func getPtrTakeOwnership() -> SwValue {

    var iegenErr = IEGenCObject()
    let result = _func_IegenExample_ValuesHolder_getPtrTakeOwnership(cself, &iegenErr)
    if iegenErr.type != nil {
      let errorType = String(cString: iegenErr.type!)
      switch errorType {
      case ("std::exception"):
        let excObj = StdException(iegenErr, true)
        ExceptionHandler.handleUncaughtException(excObj.what())
      default:
        iegenErr.type.deallocate()
        ExceptionHandler.handleUncaughtException("Uncaught Exception")
      }
    }
    var sctoswiftresult: SwValue
    sctoswiftresult = SwValue(result, true)
    return sctoswiftresult
  }

  public func getValueDefault() -> SwValue {

    var iegenErr = IEGenCObject()
    let result = _func_IegenExample_ValuesHolder_getValueDefault(cself, &iegenErr)
    if iegenErr.type != nil {
      let errorType = String(cString: iegenErr.type!)
      switch errorType {
      case ("std::exception"):
        let excObj = StdException(iegenErr, true)
        ExceptionHandler.handleUncaughtException(excObj.what())
      default:
        iegenErr.type.deallocate()
        ExceptionHandler.handleUncaughtException("Uncaught Exception")
      }
    }
    var sctoswiftresult: SwValue
    sctoswiftresult = SwValue(result, true)
    return sctoswiftresult
  }

  public func getValueCopy() -> SwValue {

    var iegenErr = IEGenCObject()
    let result = _func_IegenExample_ValuesHolder_getValueCopy(cself, &iegenErr)
    if iegenErr.type != nil {
      let errorType = String(cString: iegenErr.type!)
      switch errorType {
      case ("std::exception"):
        let excObj = StdException(iegenErr, true)
        ExceptionHandler.handleUncaughtException(excObj.what())
      default:
        iegenErr.type.deallocate()
        ExceptionHandler.handleUncaughtException("Uncaught Exception")
      }
    }
    var sctoswiftresult: SwValue
    sctoswiftresult = SwValue(result, true)
    return sctoswiftresult
  }

  public func getValueMove() -> SwValue {

    var iegenErr = IEGenCObject()
    let result = _func_IegenExample_ValuesHolder_getValueMove(cself, &iegenErr)
    if iegenErr.type != nil {
      let errorType = String(cString: iegenErr.type!)
      switch errorType {
      case ("std::exception"):
        let excObj = StdException(iegenErr, true)
        ExceptionHandler.handleUncaughtException(excObj.what())
      default:
        iegenErr.type.deallocate()
        ExceptionHandler.handleUncaughtException("Uncaught Exception")
      }
    }
    var sctoswiftresult: SwValue
    sctoswiftresult = SwValue(result, true)
    return sctoswiftresult
  }

  public func getValueReference() -> SwValue {

    var iegenErr = IEGenCObject()
    let result = _func_IegenExample_ValuesHolder_getValueReference(cself, &iegenErr)
    if iegenErr.type != nil {
      let errorType = String(cString: iegenErr.type!)
      switch errorType {
      case ("std::exception"):
        let excObj = StdException(iegenErr, true)
        ExceptionHandler.handleUncaughtException(excObj.what())
      default:
        iegenErr.type.deallocate()
        ExceptionHandler.handleUncaughtException("Uncaught Exception")
      }
    }
    var sctoswiftresult: SwValue
    sctoswiftresult = SwValue(result, true)
    return sctoswiftresult
  }

  public func getValueAutomatic() -> SwValue {

    var iegenErr = IEGenCObject()
    let result = _func_IegenExample_ValuesHolder_getValueAutomatic(cself, &iegenErr)
    if iegenErr.type != nil {
      let errorType = String(cString: iegenErr.type!)
      switch errorType {
      case ("std::exception"):
        let excObj = StdException(iegenErr, true)
        ExceptionHandler.handleUncaughtException(excObj.what())
      default:
        iegenErr.type.deallocate()
        ExceptionHandler.handleUncaughtException("Uncaught Exception")
      }
    }
    var sctoswiftresult: SwValue
    sctoswiftresult = SwValue(result, true)
    return sctoswiftresult
  }

  public func getValueAutomaticReference() -> SwValue {

    var iegenErr = IEGenCObject()
    let result = _func_IegenExample_ValuesHolder_getValueAutomaticReference(cself, &iegenErr)
    if iegenErr.type != nil {
      let errorType = String(cString: iegenErr.type!)
      switch errorType {
      case ("std::exception"):
        let excObj = StdException(iegenErr, true)
        ExceptionHandler.handleUncaughtException(excObj.what())
      default:
        iegenErr.type.deallocate()
        ExceptionHandler.handleUncaughtException("Uncaught Exception")
      }
    }
    var sctoswiftresult: SwValue
    sctoswiftresult = SwValue(result, true)
    return sctoswiftresult
  }

  public func getValueTakeOwnership() -> SwValue {

    var iegenErr = IEGenCObject()
    let result = _func_IegenExample_ValuesHolder_getValueTakeOwnership(cself, &iegenErr)
    if iegenErr.type != nil {
      let errorType = String(cString: iegenErr.type!)
      switch errorType {
      case ("std::exception"):
        let excObj = StdException(iegenErr, true)
        ExceptionHandler.handleUncaughtException(excObj.what())
      default:
        iegenErr.type.deallocate()
        ExceptionHandler.handleUncaughtException("Uncaught Exception")
      }
    }
    var sctoswiftresult: SwValue
    sctoswiftresult = SwValue(result, true)
    return sctoswiftresult
  }

  public func getRefCopy() -> SwValue {

    var iegenErr = IEGenCObject()
    let result = _func_IegenExample_ValuesHolder_getRefCopy(cself, &iegenErr)
    if iegenErr.type != nil {
      let errorType = String(cString: iegenErr.type!)
      switch errorType {
      case ("std::exception"):
        let excObj = StdException(iegenErr, true)
        ExceptionHandler.handleUncaughtException(excObj.what())
      default:
        iegenErr.type.deallocate()
        ExceptionHandler.handleUncaughtException("Uncaught Exception")
      }
    }
    var sctoswiftresult: SwValue
    sctoswiftresult = SwValue(result, true)
    return sctoswiftresult
  }

  public func getRefDefault() -> SwValue {

    var iegenErr = IEGenCObject()
    let result = _func_IegenExample_ValuesHolder_getRefDefault(cself, &iegenErr)
    if iegenErr.type != nil {
      let errorType = String(cString: iegenErr.type!)
      switch errorType {
      case ("std::exception"):
        let excObj = StdException(iegenErr, true)
        ExceptionHandler.handleUncaughtException(excObj.what())
      default:
        iegenErr.type.deallocate()
        ExceptionHandler.handleUncaughtException("Uncaught Exception")
      }
    }
    var sctoswiftresult: SwValue
    sctoswiftresult = SwValue(result, true)
    return sctoswiftresult
  }

  public func getRefMove() -> SwValue {

    var iegenErr = IEGenCObject()
    let result = _func_IegenExample_ValuesHolder_getRefMove(cself, &iegenErr)
    if iegenErr.type != nil {
      let errorType = String(cString: iegenErr.type!)
      switch errorType {
      case ("std::exception"):
        let excObj = StdException(iegenErr, true)
        ExceptionHandler.handleUncaughtException(excObj.what())
      default:
        iegenErr.type.deallocate()
        ExceptionHandler.handleUncaughtException("Uncaught Exception")
      }
    }
    var sctoswiftresult: SwValue
    sctoswiftresult = SwValue(result, true)
    return sctoswiftresult
  }

  public func getRefReference() -> SwValue {

    var iegenErr = IEGenCObject()
    let result = _func_IegenExample_ValuesHolder_getRefReference(cself, &iegenErr)
    if iegenErr.type != nil {
      let errorType = String(cString: iegenErr.type!)
      switch errorType {
      case ("std::exception"):
        let excObj = StdException(iegenErr, true)
        ExceptionHandler.handleUncaughtException(excObj.what())
      default:
        iegenErr.type.deallocate()
        ExceptionHandler.handleUncaughtException("Uncaught Exception")
      }
    }
    var sctoswiftresult: SwValue
    sctoswiftresult = SwValue(result)
    return sctoswiftresult
  }

  public func getRefAutomatic() -> SwValue {

    var iegenErr = IEGenCObject()
    let result = _func_IegenExample_ValuesHolder_getRefAutomatic(cself, &iegenErr)
    if iegenErr.type != nil {
      let errorType = String(cString: iegenErr.type!)
      switch errorType {
      case ("std::exception"):
        let excObj = StdException(iegenErr, true)
        ExceptionHandler.handleUncaughtException(excObj.what())
      default:
        iegenErr.type.deallocate()
        ExceptionHandler.handleUncaughtException("Uncaught Exception")
      }
    }
    var sctoswiftresult: SwValue
    sctoswiftresult = SwValue(result, true)
    return sctoswiftresult
  }

  public func getRefAutomaticReference() -> SwValue {

    var iegenErr = IEGenCObject()
    let result = _func_IegenExample_ValuesHolder_getRefAutomaticReference(cself, &iegenErr)
    if iegenErr.type != nil {
      let errorType = String(cString: iegenErr.type!)
      switch errorType {
      case ("std::exception"):
        let excObj = StdException(iegenErr, true)
        ExceptionHandler.handleUncaughtException(excObj.what())
      default:
        iegenErr.type.deallocate()
        ExceptionHandler.handleUncaughtException("Uncaught Exception")
      }
    }
    var sctoswiftresult: SwValue
    sctoswiftresult = SwValue(result, true)
    return sctoswiftresult
  }

  public func getRefTakeOwnership() -> SwValue {

    var iegenErr = IEGenCObject()
    let result = _func_IegenExample_ValuesHolder_getRefTakeOwnership(cself, &iegenErr)
    if iegenErr.type != nil {
      let errorType = String(cString: iegenErr.type!)
      switch errorType {
      case ("std::exception"):
        let excObj = StdException(iegenErr, true)
        ExceptionHandler.handleUncaughtException(excObj.what())
      default:
        iegenErr.type.deallocate()
        ExceptionHandler.handleUncaughtException("Uncaught Exception")
      }
    }
    var sctoswiftresult: SwValue
    sctoswiftresult = SwValue(result, true)
    return sctoswiftresult
  }

  public func getSharedRefDefault() -> ValueSharedRef {

    var iegenErr = IEGenCObject()
    let result = _func_IegenExample_ValuesHolder_getSharedRefDefault(cself, &iegenErr)
    if iegenErr.type != nil {
      let errorType = String(cString: iegenErr.type!)
      switch errorType {
      case ("std::exception"):
        let excObj = StdException(iegenErr, true)
        ExceptionHandler.handleUncaughtException(excObj.what())
      default:
        iegenErr.type.deallocate()
        ExceptionHandler.handleUncaughtException("Uncaught Exception")
      }
    }
    var sctoswiftresult: ValueSharedRef
    sctoswiftresult = ValueSharedRef(result, true)
    return sctoswiftresult
  }

  public func getSharedRefCopy() -> ValueSharedRef {

    var iegenErr = IEGenCObject()
    let result = _func_IegenExample_ValuesHolder_getSharedRefCopy(cself, &iegenErr)
    if iegenErr.type != nil {
      let errorType = String(cString: iegenErr.type!)
      switch errorType {
      case ("std::exception"):
        let excObj = StdException(iegenErr, true)
        ExceptionHandler.handleUncaughtException(excObj.what())
      default:
        iegenErr.type.deallocate()
        ExceptionHandler.handleUncaughtException("Uncaught Exception")
      }
    }
    var sctoswiftresult: ValueSharedRef
    sctoswiftresult = ValueSharedRef(result, true)
    return sctoswiftresult
  }

  public func getSharedRefMove() -> ValueSharedRef {

    var iegenErr = IEGenCObject()
    let result = _func_IegenExample_ValuesHolder_getSharedRefMove(cself, &iegenErr)
    if iegenErr.type != nil {
      let errorType = String(cString: iegenErr.type!)
      switch errorType {
      case ("std::exception"):
        let excObj = StdException(iegenErr, true)
        ExceptionHandler.handleUncaughtException(excObj.what())
      default:
        iegenErr.type.deallocate()
        ExceptionHandler.handleUncaughtException("Uncaught Exception")
      }
    }
    var sctoswiftresult: ValueSharedRef
    sctoswiftresult = ValueSharedRef(result, true)
    return sctoswiftresult
  }

  public func getSharedRefReference() -> ValueSharedRef {

    var iegenErr = IEGenCObject()
    let result = _func_IegenExample_ValuesHolder_getSharedRefReference(cself, &iegenErr)
    if iegenErr.type != nil {
      let errorType = String(cString: iegenErr.type!)
      switch errorType {
      case ("std::exception"):
        let excObj = StdException(iegenErr, true)
        ExceptionHandler.handleUncaughtException(excObj.what())
      default:
        iegenErr.type.deallocate()
        ExceptionHandler.handleUncaughtException("Uncaught Exception")
      }
    }
    var sctoswiftresult: ValueSharedRef
    sctoswiftresult = ValueSharedRef(result, true)
    return sctoswiftresult
  }

  public func getSharedRefAutomatic() -> ValueSharedRef {

    var iegenErr = IEGenCObject()
    let result = _func_IegenExample_ValuesHolder_getSharedRefAutomatic(cself, &iegenErr)
    if iegenErr.type != nil {
      let errorType = String(cString: iegenErr.type!)
      switch errorType {
      case ("std::exception"):
        let excObj = StdException(iegenErr, true)
        ExceptionHandler.handleUncaughtException(excObj.what())
      default:
        iegenErr.type.deallocate()
        ExceptionHandler.handleUncaughtException("Uncaught Exception")
      }
    }
    var sctoswiftresult: ValueSharedRef
    sctoswiftresult = ValueSharedRef(result, true)
    return sctoswiftresult
  }

  public func getSharedRefAutomaticReference() -> ValueSharedRef {

    var iegenErr = IEGenCObject()
    let result = _func_IegenExample_ValuesHolder_getSharedRefAutomaticReference(cself, &iegenErr)
    if iegenErr.type != nil {
      let errorType = String(cString: iegenErr.type!)
      switch errorType {
      case ("std::exception"):
        let excObj = StdException(iegenErr, true)
        ExceptionHandler.handleUncaughtException(excObj.what())
      default:
        iegenErr.type.deallocate()
        ExceptionHandler.handleUncaughtException("Uncaught Exception")
      }
    }
    var sctoswiftresult: ValueSharedRef
    sctoswiftresult = ValueSharedRef(result, true)
    return sctoswiftresult
  }

  public func getSharedRefTakeOwnership() -> ValueSharedRef {

    var iegenErr = IEGenCObject()
    let result = _func_IegenExample_ValuesHolder_getSharedRefTakeOwnership(cself, &iegenErr)
    if iegenErr.type != nil {
      let errorType = String(cString: iegenErr.type!)
      switch errorType {
      case ("std::exception"):
        let excObj = StdException(iegenErr, true)
        ExceptionHandler.handleUncaughtException(excObj.what())
      default:
        iegenErr.type.deallocate()
        ExceptionHandler.handleUncaughtException("Uncaught Exception")
      }
    }
    var sctoswiftresult: ValueSharedRef
    sctoswiftresult = ValueSharedRef(result, true)
    return sctoswiftresult
  }

  public func clean() -> Void {

    var iegenErr = IEGenCObject()
    _func_IegenExample_ValuesHolder_clean(cself, &iegenErr)
    if iegenErr.type != nil {
      let errorType = String(cString: iegenErr.type!)
      switch errorType {
      case ("std::exception"):
        let excObj = StdException(iegenErr, true)
        ExceptionHandler.handleUncaughtException(excObj.what())
      default:
        iegenErr.type.deallocate()
        ExceptionHandler.handleUncaughtException("Uncaught Exception")
      }
    }
  }

}

/**
 * A test example for reference_internal policy.
 */
public class AnotherValueHolder {

  public let cself: IEGenCObject
  public let owner: Bool
  private var refs: [Any]

  // internal main initializer
  internal required init(_ _cself: IEGenCObject, _ _owner: Bool = false) {
    self.cself = _cself
    self.owner = _owner
    self.refs = []
  }

  deinit {
    release_IegenExample_AnotherValueHolder(cself, owner)
  }

  public func keepIEGenReference(_ object: Any) {
    self.refs.append(object)
  }

  public convenience init() {
    var iegenErr = IEGenCObject()
    self.init(create_IegenExample_AnotherValueHolder(&iegenErr), true)
    if iegenErr.type != nil {
      let errorType = String(cString: iegenErr.type!)
      switch errorType {
      case ("std::exception"):
        let excObj = StdException(iegenErr, true)
        ExceptionHandler.handleUncaughtException(excObj.what())
      default:
        iegenErr.type.deallocate()
        ExceptionHandler.handleUncaughtException("Uncaught Exception")
      }
    }
  }

  public var value: SwValue {
    var iegenErr = IEGenCObject()
    let result = _prop_get_IegenExample_AnotherValueHolder_value(cself, &iegenErr)
    if iegenErr.type != nil {
      let errorType = String(cString: iegenErr.type!)
      switch errorType {
      case ("std::exception"):
        let excObj = StdException(iegenErr, true)
        ExceptionHandler.handleUncaughtException(excObj.what())
      default:
        iegenErr.type.deallocate()
        ExceptionHandler.handleUncaughtException("Uncaught Exception")
      }
    }
    var sctoswiftresult: SwValue
    sctoswiftresult = SwValue(result)
    sctoswiftresult.keepIEGenReference(self)
    return sctoswiftresult
  }

  public var valueReference: SwValue {
    var iegenErr = IEGenCObject()
    let result = _prop_get_IegenExample_AnotherValueHolder_valueReference(cself, &iegenErr)
    if iegenErr.type != nil {
      let errorType = String(cString: iegenErr.type!)
      switch errorType {
      case ("std::exception"):
        let excObj = StdException(iegenErr, true)
        ExceptionHandler.handleUncaughtException(excObj.what())
      default:
        iegenErr.type.deallocate()
        ExceptionHandler.handleUncaughtException("Uncaught Exception")
      }
    }
    var sctoswiftresult: SwValue
    sctoswiftresult = SwValue(result)
    return sctoswiftresult
  }

  public var sharedValue: ValueSharedRef {
    var iegenErr = IEGenCObject()
    let result = _prop_get_IegenExample_AnotherValueHolder_sharedValue(cself, &iegenErr)
    if iegenErr.type != nil {
      let errorType = String(cString: iegenErr.type!)
      switch errorType {
      case ("std::exception"):
        let excObj = StdException(iegenErr, true)
        ExceptionHandler.handleUncaughtException(excObj.what())
      default:
        iegenErr.type.deallocate()
        ExceptionHandler.handleUncaughtException("Uncaught Exception")
      }
    }
    var sctoswiftresult: ValueSharedRef
    sctoswiftresult = ValueSharedRef(result, true)
    return sctoswiftresult
  }

  public var sharedValueReference: ValueSharedRef {
    var iegenErr = IEGenCObject()
    let result = _prop_get_IegenExample_AnotherValueHolder_sharedValueReference(cself, &iegenErr)
    if iegenErr.type != nil {
      let errorType = String(cString: iegenErr.type!)
      switch errorType {
      case ("std::exception"):
        let excObj = StdException(iegenErr, true)
        ExceptionHandler.handleUncaughtException(excObj.what())
      default:
        iegenErr.type.deallocate()
        ExceptionHandler.handleUncaughtException("Uncaught Exception")
      }
    }
    var sctoswiftresult: ValueSharedRef
    sctoswiftresult = ValueSharedRef(result, true)
    return sctoswiftresult
  }

}
