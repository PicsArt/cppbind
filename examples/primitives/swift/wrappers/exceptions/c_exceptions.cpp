/**
 * ,--.,------. ,----.   ,------.,--.  ,--. 
 * |  ||  .---''  .-.|   |  .---'|  ,'.|  | 
 * |  ||  `--, |  | .---.|  `--, |  |' '  | 
 * |  ||  `---.'  '--'  ||  `---.|  | `   | 
 * `--'`------' `------' `------'`--'  `--' 
 * 
 * This file is generated by iegen on 07/20/2021-09:52.
 * Please do not change it manually.
 */

#include <string.h>
#include <memory>
#include "swift/wrappers/exceptions/c_exceptions.h"
#include "cxx/exceptions/exceptions.hpp"


using namespace pi::video_engine::errors;

void release_SystemError(void* _Nonnull cself) {
    delete dynamic_cast<pi::video_engine::errors::SystemError*>(static_cast<std::exception*>(cself));
}

void* _Nonnull create_SystemError(char* _Nonnull message){
    auto c_to_cxx_message = std::string(message);
    free(message);
    auto this_object = new pi::video_engine::errors::SystemError(c_to_cxx_message);
    return static_cast<std::exception*>(this_object);
}

const char * _Nonnull _func_SystemError_what(void* _Nonnull cself, ErrorObj* _Nonnull err){
    auto c_to_cxx_cself = dynamic_cast<pi::video_engine::errors::SystemError*>(static_cast<std::exception*>(cself));
    try {
      const auto& result = c_to_cxx_cself->what();
      
      return result;
    }
    catch (const std::exception& e) {
        err->is_err = true;
        err->err_type = 1;
        err->err_ptr = new std::exception(e);
    }
    catch (...) {
        err->is_err = true;
    }
    const char * result;
    return result;
}

void release_FileError(void* _Nonnull cself) {
    delete dynamic_cast<pi::video_engine::errors::FileError*>(static_cast<std::exception*>(cself));
}

void* _Nonnull create_FileError(char* _Nonnull message){
    auto c_to_cxx_message = std::string(message);
    free(message);
    auto this_object = new pi::video_engine::errors::FileError(c_to_cxx_message);
    return static_cast<std::exception*>(this_object);
}

const char * _Nonnull _func_FileError_what(void* _Nonnull cself, ErrorObj* _Nonnull err){
    auto c_to_cxx_cself = dynamic_cast<pi::video_engine::errors::FileError*>(static_cast<std::exception*>(cself));
    try {
      const auto& result = c_to_cxx_cself->what();
      
      return result;
    }
    catch (const std::exception& e) {
        err->is_err = true;
        err->err_type = 1;
        err->err_ptr = new std::exception(e);
    }
    catch (...) {
        err->is_err = true;
    }
    const char * result;
    return result;
}

void release_SimpleBaseException(void* _Nonnull cself) {
    delete dynamic_cast<pi::video_engine::errors::SimpleBaseException*>(static_cast<pi::video_engine::errors::SimpleBaseException*>(cself));
}

void* _Nonnull create_SimpleBaseException(int err_num){
    
    auto this_object = new pi::video_engine::errors::SimpleBaseException(err_num);
    return static_cast<pi::video_engine::errors::SimpleBaseException*>(this_object);
}

int _func_SimpleBaseException_errNum(void* _Nonnull cself, ErrorObj* _Nonnull err){
    auto c_to_cxx_cself = dynamic_cast<pi::video_engine::errors::SimpleBaseException*>(static_cast<pi::video_engine::errors::SimpleBaseException*>(cself));
    try {
      const auto& result = c_to_cxx_cself->errNum();
      
      return result;
    }
    catch (const std::exception& e) {
        err->is_err = true;
        err->err_type = 1;
        err->err_ptr = new std::exception(e);
    }
    catch (...) {
        err->is_err = true;
    }
    int result;
    return result;
}

void release_SimpleChildException(void* _Nonnull cself) {
    delete dynamic_cast<pi::video_engine::errors::SimpleChildException*>(static_cast<pi::video_engine::errors::SimpleBaseException*>(cself));
}

void* _Nonnull create_SimpleChildException(int err_num){
    
    auto this_object = new pi::video_engine::errors::SimpleChildException(err_num);
    return static_cast<pi::video_engine::errors::SimpleBaseException*>(this_object);
}

int _func_SimpleChildException_errNum(void* _Nonnull cself, ErrorObj* _Nonnull err){
    auto c_to_cxx_cself = dynamic_cast<pi::video_engine::errors::SimpleChildException*>(static_cast<pi::video_engine::errors::SimpleBaseException*>(cself));
    try {
      const auto& result = c_to_cxx_cself->errNum();
      
      return result;
    }
    catch (const std::exception& e) {
        err->is_err = true;
        err->err_type = 1;
        err->err_ptr = new std::exception(e);
    }
    catch (...) {
        err->is_err = true;
    }
    int result;
    return result;
}

using namespace pi::video_engine::exceptions;

void release_Integer(void* _Nonnull cself) {
    delete dynamic_cast<pi::video_engine::exceptions::Integer*>(static_cast<pi::video_engine::exceptions::Integer*>(cself));
}

void* _Nonnull create_Integer(int n){
    
    auto this_object = new pi::video_engine::exceptions::Integer(n);
    return static_cast<pi::video_engine::exceptions::Integer*>(this_object);
}
int _prop_get_Integer_value(void* _Nonnull cself){
    auto c_to_cxx_cself = dynamic_cast<pi::video_engine::exceptions::Integer*>(static_cast<pi::video_engine::exceptions::Integer*>(cself));
    auto result = c_to_cxx_cself->value();
    
    return result;
}

void release_Exc(void* _Nonnull cself) {
    delete dynamic_cast<pi::video_engine::exceptions::Exc*>(static_cast<pi::video_engine::exceptions::Exc*>(cself));
}

int _func_Exc_getByKey(CDataMap m, int key, ErrorObj* _Nonnull err){
    std::map<int, int> c_to_cxx_m;

    for (size_t _i = 0; _i < m.size; ++_i) {
        auto ktmp_m = reinterpret_cast<int*>(m.keys)[_i];
        auto vtmp_m = reinterpret_cast<int*>(m.values)[_i];
        
        
        c_to_cxx_m.insert({ ktmp_m, vtmp_m });
    }
  
    
  
    try {
      const auto& result = pi::video_engine::exceptions::Exc::getByKey(c_to_cxx_m, key);
      
      return result;
    }catch (const std::out_of_range& e) {
        err->is_err = true;
        err->err_type = 1;
        err->err_ptr = new std::out_of_range(e);
    }catch (const std::invalid_argument& e) {
        err->is_err = true;
        err->err_type = 2;
        err->err_ptr = new std::invalid_argument(e);
    }catch (const std::length_error& e) {
        err->is_err = true;
        err->err_type = 3;
        err->err_ptr = new std::length_error(e);
    }catch (const pi::video_engine::errors::SystemError& e) {
        err->is_err = true;
        err->err_type = 4;
        err->err_ptr = new pi::video_engine::errors::SystemError(e);
    }
    catch (const std::exception& e) {
        err->is_err = true;
        err->err_type = 5;
        err->err_ptr = new std::exception(e);
    }
    catch (...) {
        err->is_err = true;
    }
    int result;
    return result;
}

void _func_Exc_noop(ErrorObj* _Nonnull err){
    try {
      pi::video_engine::exceptions::Exc::noop();
    }
    catch (const std::exception& e) {
        err->is_err = true;
        err->err_type = 1;
        err->err_ptr = new std::exception(e);
    }
    catch (...) {
        err->is_err = true;
    }
}

void* _Nonnull _func_Exc_returnInteger(bool do_throw, ErrorObj* _Nonnull err){
    
  
    try {
      const auto& result = pi::video_engine::exceptions::Exc::returnInteger(do_throw);
      auto value_ptr_result = const_cast<pi::video_engine::exceptions::Integer*>(result);
    auto cxx_to_c_result = static_cast<pi::video_engine::exceptions::Integer*>(value_ptr_result);
      return cxx_to_c_result;
    }catch (const std::out_of_range& e) {
        err->is_err = true;
        err->err_type = 1;
        err->err_ptr = new std::out_of_range(e);
    }
    catch (const std::exception& e) {
        err->is_err = true;
        err->err_type = 2;
        err->err_ptr = new std::exception(e);
    }
    catch (...) {
        err->is_err = true;
    }
    void* result;
    return result;
}

void _func_Exc_raiseErrorByType(char* _Nonnull err_type, ErrorObj* _Nonnull err){
    auto c_to_cxx_err_type = std::string(err_type);
    free(err_type);
  
    try {
      pi::video_engine::exceptions::Exc::raiseErrorByType(c_to_cxx_err_type);
    }catch (const std::runtime_error& e) {
        err->is_err = true;
        err->err_type = 1;
        err->err_ptr = new std::runtime_error(e);
    }catch (const pi::video_engine::errors::FileError& e) {
        err->is_err = true;
        err->err_type = 2;
        err->err_ptr = new pi::video_engine::errors::FileError(e);
    }catch (const pi::video_engine::errors::SystemError& e) {
        err->is_err = true;
        err->err_type = 3;
        err->err_ptr = new pi::video_engine::errors::SystemError(e);
    }catch (const pi::video_engine::errors::SimpleChildException& e) {
        err->is_err = true;
        err->err_type = 4;
        err->err_ptr = new pi::video_engine::errors::SimpleChildException(e);
    }catch (const pi::video_engine::errors::SimpleBaseException& e) {
        err->is_err = true;
        err->err_type = 5;
        err->err_ptr = new pi::video_engine::errors::SimpleBaseException(e);
    }catch (const std::exception& e) {
        err->is_err = true;
        err->err_type = 6;
        err->err_ptr = new std::exception(e);
    }
    catch (...) {
        err->is_err = true;
    }
}