type_vars:
  - type: "pi::video_engine::yaml_test::WithExternalAPIComments"
    ::
    - type: "retInt(int)"
      vars:
        action: gen_method
        throws: no_throw

    - type: "setStr(const std::string &)"
      vars:
        action: gen_setter
        throws: no_throw

    # enrty with duplicate key
    - type: "setStr(const std::string &)"
      vars:
        action: gen_setter
        throws: no_throw
