gen_actions:
  - type: pi
    sub:
      - type: video_engine
        sub:
          - type: "yaml_test::WithExternalAPIComments"
            sub:
              # entry with this key is already defined in ./rules_1.yaml file
              - type: "retInt(int)"
                rule:
                  gen: method
                  throws: no_throw
